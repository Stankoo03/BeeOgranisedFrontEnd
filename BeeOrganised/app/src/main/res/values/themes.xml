<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BeeOrganised" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->

        <item name="colorPrimary">@color/UserChosing</item>
        <item name="colorPrimaryVariant">@color/BehindCalendar</item>


        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.BeeOrganised.Fullscreen" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:actionBarStyle">@style/Widget.Theme.BeeOrganised.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>
    <style name="Theme.BeeOrganised.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.BeeOrganised.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.BeeOrganised.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>